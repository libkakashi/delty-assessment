'use client';

import ChatSidebar from '~/components/ChatSidebar';

export default function ChatPage() {
  return (
    <div className="h-screen w-full">
      <ChatSidebar className="w-full border-l-0" />
    </div>
  );
}
